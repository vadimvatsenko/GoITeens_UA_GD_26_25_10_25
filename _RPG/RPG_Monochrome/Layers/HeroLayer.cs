using RPG_Monochrome.Engine;

namespace RPG_Monochrome.Layers;

public class HeroLayer : Layer
{
    public HeroLayer(int maxWidth, int maxHeight, Renderer renderer) : base(maxWidth, maxHeight, renderer)
    {
    }
}