using RPG_Monochrome.Engine;

namespace RPG_Monochrome.Layers;

public class BackGroundLayer : Layer
{
    public BackGroundLayer(int maxWidth, int maxHeight, Renderer renderer) : base(maxWidth, maxHeight, renderer)
    {
        
    }
}