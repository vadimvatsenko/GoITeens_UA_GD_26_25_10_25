using RPG_Monochrome.Engine;

namespace RPG_Monochrome.Layers;

public class WallLayer : Layer
{
    public WallLayer(int maxWidth, int maxHeight, Renderer renderer) : base(maxWidth, maxHeight, renderer)
    {
    }
}